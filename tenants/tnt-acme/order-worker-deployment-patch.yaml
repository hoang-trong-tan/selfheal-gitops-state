apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-worker
  namespace: sample-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order-worker
  template:
    metadata:
      labels:
        app: order-worker
    spec:
      containers:
      - name: worker
        image: nginx:alpine